apiVersion: v1
kind: ConfigMap
metadata:
  name: global-config
  namespace: application
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "DEBUG"
  ALLOWED_ORIGINS: "https://example.com,http://localhost:3000"
  ENCRYPTION_KEY: "-----BEGIN KEY-----\nABC123\n-----END KEY-----"